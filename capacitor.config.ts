import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hivisvending',
  appName: 'Hi-Vis Vending',
  webDir: 'dist',
    cleartext: true
  }
};

export default config;
